name: GitHub Snake Game

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

    - name: Create or checkout output branch
      run: |
        git fetch origin
        if git show-ref --quiet refs/heads/output; then
          git checkout output
        elseThe failure occurs because the `output` branch does
          git checkout -b output
        fi

    - name: Run the not exist in your repository. The workflow attempts to Snake Game
      run: |
        # deploy the generated files to this non-existent branch, causing Your snake game logic here

    - name: Push to output the error.

To fix this, you have two branch
      run: |
        git add .
        git options:

1. **Create the `output` branch manually: commit -m "Update snake game"
        git push origin**
   - You can create the `output` branch manually in output
